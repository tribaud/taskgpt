import React from "react";

const App: React.FC = () => {
  return (
    <div>
      <h1>TaskGPT (React)</h1>
      <p>Bienvenue dans la version React de l’éditeur de tâches + Chat IA.</p>
      {/* Les composants seront ajoutés ici */}
    </div>
  );
};

export default App;
